let friends: string[] = [`talha` , `fakhir` , `raffay` , `amish`];
let message: string = `You are invited for a dinner tomorrow `;
for (let i=0;i<friends.length; i++){
     console.log( message + friends[i]);
}